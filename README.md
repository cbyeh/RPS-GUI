# A rock paper scissors interactive game with Java's Swing

Run with:

`git clone https://github.com/cbyeh/RPS-GUI.git`

`cd RPS-GUI`

`javac -d out src/RPSGUIGame.java src/RPSGame.java && java -classpath out RPSGUIGame`
